# 采集

# 调试
#setblock ~ ~10 ~ minecraft:coal_ore replace

clone ~ ~ ~ ~ ~ ~ ~ ~10 ~ masked normal

#loot spawn ~ ~ ~ mine ~ ~ ~ mainhand
#setblock ~ ~ ~ minecraft:air replace
setblock ~ ~ ~ minecraft:air destroy